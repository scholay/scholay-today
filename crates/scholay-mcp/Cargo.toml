[package]
name = "scholay-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
rmcp = { version = "=3.2.0", default-features = false, features = ["server", "transport-io"] }
serde_json.workspace = true
tokio.workspace = true
anyhow.workspace = true
scholay-local-ipc = { path = "../scholay-local-ipc" }

[dev-dependencies]
tempfile = "3"
